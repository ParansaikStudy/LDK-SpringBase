server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/SpringBasedb?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: dbzoseh2rl
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml